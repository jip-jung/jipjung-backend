<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jipjung.project.repository.UserMapper">

    <!-- Result Map (Full) -->
    <resultMap id="UserResultMap" type="com.jipjung.project.domain.User">
        <id property="id" column="user_id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="nickname" column="nickname"/>
        <result property="phone" column="phone"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="socialProvider" column="social_provider"/>
        <result property="socialId" column="social_id"/>
        <result property="role" column="role" javaType="com.jipjung.project.domain.UserRole"/>
        <result property="isActive" column="is_active"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="onboardingCompleted" column="onboarding_completed"/>
        <result property="birthYear" column="birth_year"/>
        <result property="annualIncome" column="annual_income"/>
        <result property="existingLoanMonthly" column="existing_loan_monthly"/>
        <result property="currentAssets" column="current_assets"/>
        <result property="dsrMode" column="dsr_mode"/>
        <result property="lastDsrCalculationAt" column="last_dsr_calculation_at"/>
        <result property="cachedMaxLoanAmount" column="cached_max_loan_amount"/>
        <result property="currentLevel" column="current_level"/>
        <result property="currentExp" column="current_exp"/>
        <result property="streakCount" column="streak_count"/>
        <result property="lastStreakDate" column="last_streak_date"/>
        <result property="maxStreak" column="max_streak"/>
        <result property="selectedThemeId" column="selected_theme_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- ID로 활성 사용자 조회 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT *
        FROM `user`
        WHERE user_id = #{userId}
          AND is_deleted = false
    </select>

    <!-- ID로 활성 사용자 조회 + row lock -->
    <select id="findByIdForUpdate" resultMap="UserResultMap">
        SELECT *
        FROM `user`
        WHERE user_id = #{userId}
          AND is_deleted = false
        FOR UPDATE
    </select>

    <!-- 이메일로 사용자 조회 -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT *
        FROM `user`
        WHERE email = #{email}
          AND is_deleted = false
          AND (is_active = true OR is_active IS NULL)
    </select>

    <!-- 사용자 등록 -->
    <insert id="insertUser" parameterType="com.jipjung.project.domain.User"
            useGeneratedKeys="true" keyProperty="id" keyColumn="user_id">
        INSERT INTO `user` (email, nickname, password, role)
        VALUES (#{email}, #{nickname}, #{password}, #{role})
    </insert>

    <!-- 이메일 중복 체크 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM `user`
        WHERE email = #{email}
    </select>

    <!-- 온보딩 정보 업데이트 (currentAssets 추가) -->
    <update id="updateOnboarding">
        UPDATE `user`
        SET birth_year = #{birthYear},
            annual_income = #{annualIncome},
            existing_loan_monthly = #{existingLoanMonthly},
            current_assets = #{currentAssets},
            onboarding_completed = true,
            updated_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND is_deleted = false
    </update>

    <!-- 프로필 정보 업데이트 -->
    <update id="updateProfile">
        UPDATE `user`
        SET nickname = #{nickname},
            annual_income = #{annualIncome},
            existing_loan_monthly = #{existingLoanMonthly},
            updated_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND is_deleted = false
    </update>

</mapper>
